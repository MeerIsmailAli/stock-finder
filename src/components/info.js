const Info = () => {
    return (
      <div className="h-full w-full">
        <p>this website uses the polygio.io API to get real time stock updates(candles) for the past 100 days which includes the opening and closing of market</p>
      </div>
    );
  };
  
  export default Info;
  